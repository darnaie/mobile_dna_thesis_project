import pandas as pd\n\n# Function to read taxonomy file and process species names\ndef process_taxonomy_file(filepath):\n    taxonomy_df = pd.read_csv(filepath, sep='\t')\n    taxonomy_df['species'] = taxonomy_df['original_name'].apply(lambda x: remove_prefix(x))\n    taxonomy_df['gca'] = taxonomy_df['gca_id']\n    return taxonomy_df[['species', 'gca']]\n\n# Utility function to remove prefixes from species names\ndef remove_prefix(name):\n    return name.split('_')[-1]  # Removes prefix by splitting\n\n# Main function to identify host species\ndef identify_host_species(taxonomy_file):\n    hosts = process_taxonomy_file(taxonomy_file)\n    return hosts\n\n# Example usage\nif __name__ == '__main__':\n    taxonomy_file = 'bac120_taxonomy.tsv'  # Specify the path to the taxonomy file\n    host_species = identify_host_species(taxonomy_file)\n    print(host_species)